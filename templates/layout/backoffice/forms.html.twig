{% extends '@backend/base.html.twig' %}

{% block breadcrumb %}
    <div>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb breadcrumb-style1 mg-b-10">
                <li class="breadcrumb-item"><a href="{{ path('front_index') }}">Tableau de bord</a></li>
                <li class="breadcrumb-item"><a href="{{ path(vm.routes.index) }}">{{ vm.formattedName | trans }}</a>
                </li>
            </ol>
        </nav>
        <h1 class="mg-b-0 tx-spacing--1 tx-bolder">
            {{ vm.data.id ? 'Édition ' : 'Création ' }}{{ vm.formattedName | capitalize }}
        </h1>
    </div>
{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="card">
            <div class="card-body">
                {% block content %}
                    {{ include('@backend/layout/_form.html.twig') }}
                {% endblock %}
            </div>
        </div>
    </div>
{% endblock %}
