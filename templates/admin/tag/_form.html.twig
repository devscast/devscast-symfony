{{ form_start(form) }}
    <div class="pl-lg-4">
        <div class="row">
            <div class="col-lg-12">{{ form_row(form.name) }}</div>
        </div>
        <button class="btn btn-block btn-primary">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
