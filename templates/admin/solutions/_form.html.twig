{{ form_start(form) }}
    <div class="pl-lg-4">
        <div class="row">
            <div class="col-lg-6">{{ form_row(form.user) }}</div>
            <div class="col-lg-6">{{ form_row(form.challenge) }}</div>
        </div>
        <div class="row">
            <div class="col-lg-12">{{ form_row(form.content) }}</div>
        </div>
        <button class="btn btn-block btn-primary">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
