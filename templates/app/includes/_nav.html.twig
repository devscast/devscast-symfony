{% set _route = app.request.get('_route') %}

<nav class="navbar navbar-expand-lg navbar-light bg-black">
    <div class="container">
        <a class="navbar-brand" href="{{ path('home') }}">
        </a>
        <button
                class="navbar-toggler"
                type="button"
                data-toggle="collapse"
                data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent"
                aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a title="contact"
                       class="nav-link {{ _route == "home" ? 'active' : '' }}"
                       href="{{ path('home') }}">
                        Accueil
                    </a>
                </li>
                <li class="nav-item">
                    <a title="projects"
                       class="nav-link {{ _route == "app_project_index" or _route == "app_project_show" ? "active" : "" }}"
                       href="{{ path("app_project_index") }}">
                        Projets
                    </a>
                </li>
                <li class="nav-item">
                    <a title="Videos"
                       href="{{ path('app_post_index') }}"
                       class="nav-link {{ _route == "app_post_index" or _route == "app_post_show" ? "active" : "" }}">
                        Vidéos
                    </a>
                </li>
                <li class="nav-item">
                    <a title="blog"
                       class="nav-link {{ _route == "app_blog_index" or _route == "app_blog_show" ? 'active' : '' }}"
                       href="{{ path('app_blog_index') }}">
                        Blog
                    </a>
                </li>
                <li class="nav-item">
                    <a title="contact"
                       class="nav-link {{ _route == "contact" ? 'active' : '' }}"
                       href="{{ path('contact') }}">
                        Contact
                    </a>
                </li>
                <li class="nav-item ml-lg-5">
                    <div class="navbar-search">
                        <form action="{{ path("app_post_search") }}" method="get" class="search-form">
                            <label for="q" class="screen-reader-text">Rechercher :</label>
                            <input type="search" id="q" name="q" class="search-field" placeholder="search...">
                            <button type="submit"><i class="fas fa-search"></i></button>
                        </form>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>
